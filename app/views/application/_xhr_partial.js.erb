ujs.closeBsModals();
ujs.closeRemodals();

if ($('#<%= dashed(params[:partial]) %>').length) {
  ujs.html('#flashes', '<%= j render("flashes") %>');
  ujs.replace('#<%= dashed(params[:partial]) %>', '<%= j render(params[:partial]) %>');
} else {
  ujs.visit('<%= url %>');
}
